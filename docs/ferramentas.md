# Ferramentas

SEMA MT: APF Rural expõe 1 ferramenta (todas somente leitura).

### 1. `sema_mt_apf_consultar`
**Input**: `cpf_proprietario` (opcional), `cnpj_proprietario` (opcional), `cpf_responsavel` (opcional), `apf` (opcional), `car` (opcional)

SEMA MT: APF Rural, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEMA MT: APF Rural
```
